apiVersion: v1
kind: Service
metadata:
  name: flask-backend-service
  namespace: flask-sprint
spec:
  selector:
    app: flask-backend
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080
  type: NodePort
